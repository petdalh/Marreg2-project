import numpy as np

def create_R(psi):
    """Create rotation matrix from heading angle."""
    return np.array([
        [np.cos(psi), -np.sin(psi), 0],
        [np.sin(psi), np.cos(psi), 0],
        [0, 0, 1]
    ])